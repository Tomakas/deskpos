name: maty
description: "Maty — offline-first POS system"
publish_to: 'none'
version: 0.7.6

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

  # State management
  flutter_riverpod: ^2.6.1

  # Database
  drift: ^2.30.0
  drift_flutter: ^0.2.8
  sqlite3_flutter_libs: ^0.5.28

  # Immutable models
  freezed_annotation: ^3.1.0

  # Routing
  go_router: ^14.8.1

  # Backend
  supabase_flutter: ^2.8.4

  # Printing
  pdf: ^3.11.1
  printing: ^5.13.4

  # Charts
  fl_chart: ^1.1.1

  # Utilities
  uuid: ^4.5.1
  crypto: ^3.0.6
  path_provider: ^2.1.5
  path: ^1.9.1
  shared_preferences: ^2.3.4
  share_plus: ^10.1.4
  package_info_plus: ^8.0.0
  web: ^1.1.0

dependency_overrides:
  path_provider_foundation: 2.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # Code generation
  build_runner: ^2.4.14
  drift_dev: ^2.30.0
  freezed: ^3.2.5

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/fonts/
