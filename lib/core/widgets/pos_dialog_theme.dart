abstract final class PosDialogTheme {
  static const double padding = 24.0;
  static const double sectionSpacing = 24.0;
  static const double fieldSpacing = 12.0;
  static const double actionHeight = 44.0;
  static const double actionSpacing = 8.0;
  static const double actionFontSize = 13.0;

  // Numpad — Large (login, opening cash, cash movement, loyalty, credit, lock, switch user)
  static const double numpadLargeHeight = 64.0;
  static const double numpadLargeRadius = 12.0;
  static const double numpadLargeGap = 8.0;
  static const double numpadLargeFontSize = 24.0;

  // Numpad — Compact (change total, discount, voucher create)
  static const double numpadCompactHeight = 48.0;
  static const double numpadCompactRadius = 8.0;
  static const double numpadCompactGap = 4.0;
  static const double numpadCompactFontSize = 20.0;
}
